mod module;

class Vec<T> {
	len: usize,
	cap: usize,
	ptr: *T,

	fn push(&self, item: T) {
		*self.ptr = item;
	}
}

fn main(argc: i32, argv: **i8) -> i32 {
	let v = Vec<i32> {};

	v.push(4);

	return 0;
}
