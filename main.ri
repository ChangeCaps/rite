mod module;
mod vec;

class Vec<T> {
	len: usize,
	cap: usize,
	ptr: *T,

	fn push(&self, item: T) {
		self.ptr = bitcast(&self.len);
		self.ptr = null;
	}
}

fn main(argc: i32, argv: **i8) -> i32 {
	let v = Vec {};

	v.push(4);

	return 0;
}
